server:
  port: 8081
#  servlet:
#    contextPath: /api/v3

spring:
  application:
    name: multi-module
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:mysql://localhost:3306/cache_schema
    username: cache_user
    password: User@Cache123
    driver-class-name: com.mysql.cj.jdbc.Driver

#security:
#  oauth2:
#    token-uri: jdbc:mysql://localhost:3306/cache_schema


#Swagger: API documentation
swagger:
  api:
    title: General services @${spring.profiles.active}
    description: General REST Services API Documentation
    version: 1
    api-docs-path: /v2/api-docs
  enabled: true
